{% apply inline_css %}
    <style>
        .mail-body {
			color: #254da3;
		}
		.container-img{
			text-align: center;
		}
		img{
			display: block;
   			margin: 0 auto;
		}
		h1{
			text-align: center;
		}
        p{
            margin: 10px 25px;
        }
    </style>
    <div class="mail-body">
        <div class="container-img">
			<img src="{{ email.image('@images/logo-usb.png') }}" alt="Logo club USB" width="100" height="auto">
		</div>
        <h1>{{ title }}</h1>
        <p>{{ content }}</p>
        <p>USB Tir à l'arc</p>
    </div>
{% endapply %}