<form method="post" action="{{ path('app_back_course_delete', {'id': course.id}) }}" onsubmit="return confirm('Êtes-vous sûr de suppimer cet objet ?');" class="my-2">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ course.id) }}">
    <button class="btn">Supprimer</button>
</form>
